

整理完資料夾後，主管突然急急忙忙走過來：
*「不好了！我們剛剛收到風聲，住在花蓮（Hualien）的客戶資料好像出了點問題，還有，我想知道有哪些大客戶的業績（Sales）是好幾萬以上的。但這份台灣客戶清單有幾百行，我用 Excel 滾輪滾到手快抽筋了，你能幫我一秒抓出來嗎？」*

沒問題！在 Linux 裡，我們不需要打開厚重的文書軟體，兩行指令直接搞定！

---

### 1. 偷看檔案內容：這份大檔案長怎樣？
在 Linux 中，如果想直接在終端機「印出」整個文字檔的內容，我們使用 `cat`（Concatenate）指令。請輸入以下指令並按下 **Enter**：

```bash
cat taiwan_customers.txt
```{{execute}}

> 💡 **哇！畫面被洗版了！**
> 畫面上快速刷出一大串客戶資料對吧？這就是 `cat` 的威力，它會把檔案內容毫無保留地全部倒出來。但如果檔案有幾萬行，洗版反而很難找資料。所以接下來我們要搭配「篩選濾網」。

---

### 2. 關鍵字百步穿楊（Grep）：一秒搜出「花蓮」客戶
我們使用 **`grep`** 指令，它就像是 Linux 內建的 `Ctrl + F` 搜尋功能。我們告訴它我們要找的關鍵字是 `Hualien`（花蓮）：

```bash
grep "Hualien" taiwan_customers.txt
```{{execute}}

> 💡 **發現神奇的事了嗎？**
> 剛才幾百行的混亂資料消失了！畫面上只精準留下了 Region（地區）欄位是 **Hualien** 的客戶名單！主管找的就是這群人！

---

### 3. 進階連鎖技（Pipe 🚀）：找出特定名字的客戶
主管追加需求：*「對了！裡面好像有一個叫 User_50 的人，幫我看看他的資料！」*
我們可以利用 Linux 最厲害的「管線符號 `|`」，把前一個指令的結果直接丟給下一個指令當濾網：

```bash
cat taiwan_customers.txt | grep "User_50"
```{{execute}}

> 💡 **這行指令代表什麼意思？**
> `|` 叫做管線（Pipe）。這行指令的意思是：「請 `cat` 把所有客戶倒出來，但**先別印在畫面上**，直接通過管線 `|` 丟給 `grep` 去過濾出含有 `User_50` 的那一行！」
> 
> 執行後，畫面上是不是只剩下一行精準的資料了？這就是 Linux 處理大量數據時的核心魔法！

---

### 🏁 第三關任務達成！
你已經學會了文字搜查大絕招 `cat` 與 `grep`，以後面對幾十萬行的日誌（Log）或報表也完全不用怕了！

請點擊右下角 **NEXT** 按鈕，我們前往最後一關，解開一開始看到的「權限符號暗號」！**
