# Step 8：看懂檔案內容——cat 與 less

## 🔹 指令：cat——把內容整份印出來

`cat` 是 *Concatenate*（串接）的縮寫，最常見的用法是直接把檔案內容印在畫面上：

`cat part_time_weekly_report.txt`{{execute}}

你會看到整份工讀生週報的內容直接顯示出來。

## ⚠️ cat 的缺點：檔案太長會洗版

`cat` 適合看「短檔案」，但如果檔案有幾百、幾千行，內容會一口氣全部噴出來，你只能眼睁睁看著畫面被洗版，根本來不及看清楚。我們來實際體驗一下：

`cat download/customers.zip`{{execute}}

你會看到一堆亂碼——這是因為 `cat` 是設計來顯示「文字內容」的，`.zip` 是壓縮過的二進位檔案，硬用 `cat` 打開只會看到無意義的符號。這也順便提醒我們：**不是所有檔案都適合用 `cat` 打開。**

按 `Ctrl + C` 可以隨時中斷正在執行的指令（如果畫面卡住或洗版太誇張，這是你的緊急煞車鍵）。

## 🔹 指令：less——一頁一頁慢慢看

面對長檔案，更好的做法是用 `less`，它會把內容「分頁」顯示，你可以慢慢往下捲動：

`less part_time_weekly_report.txt`{{execute}}

進入 `less` 之後，常用操作如下：

| 按鍵 | 作用 |
|---|---|
| 空白鍵 / `f` | 往下翻一頁 |
| `b` | 往上翻一頁 |
| 方向鍵 ↑↓ | 一行一行捲動 |
| `/關鍵字` | 搜尋這個關鍵字（按 Enter 確認） |
| `q` | 離開，回到終端機 |

請按 `q` 離開 `less` 畫面，回到終端機提示字元。

## 🆚 cat 跟 less 該用哪一個？

| 情境 | 該用 |
|---|---|
| 檔案很短（幾行到幾十行），想一次看完 | `cat` |
| 檔案很長，想慢慢捲、想搜尋特定內容 | `less` |

## ✅ 過關檢核

- [ ] 我已經用 `cat` 看過一份純文字檔案的內容
- [ ] 我已經用 `less` 打開檔案，並用空白鍵翻頁、用 `q` 離開
- [ ] 我知道 `Ctrl+C` 是緊急中斷指令的快捷鍵

下一站，我們要學會把檔案「複製一份」出去。
